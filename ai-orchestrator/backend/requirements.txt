fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.0
pydantic-settings==2.5.0
websockets==13.1
# httpx must be pinned to match what the ollama library expects
httpx==0.27.2
python-dotenv==1.0.0

# Ollama client — 0.4.4+ fixes positional-arg httpx incompatibility (GitHub Issue #17)
ollama==0.4.4

# LangGraph workflow
langgraph==0.2.28

# RAG & Vector Store (Phase 3)
chromadb==0.5.0
numpy<2.0.0
pypdf==4.2.0
tiktoken==0.7.0

# Google Gemini — used for dashboard generation and vision analysis
google-generativeai==0.8.4

# Misc
pyyaml==6.0.2

# Vision support — PIL is needed by VisionAgent to decode JPEG camera snapshots
# before sending them to Gemini Vision API
Pillow>=10.0.0
